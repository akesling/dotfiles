# Let npm use its default prefix. Add per-machine overrides in
# ~/.local/dotfiles/ if needed (npm doesn't support `include`, so set
# environment variables in the shell rc husk instead, e.g. `npm_config_prefix`).
